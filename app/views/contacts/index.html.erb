<div class="container">
  <div class="row">
    <div class="col-sm-12">
      <div class="contact d-flex flex-column align-items-center">
        <% @contacts.each do |contact| %>
          <div class="affiche">
            <input class="form-checkbox" type="checkbox" value="" id="defaultCheck1">
            <p class="flex-grow-1"><%= contact.content %></p>
            <div class="inf d-flex flex-column justify-content-center">
              <p><%= contact.email %></p>
              <P><%= contact.created_at %></p>
            </div>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>
