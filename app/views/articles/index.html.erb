<div class="container">
  <div class="row">
    <div class="col-sm-12">
      <div class="article">
        <div>
          <% @articles.each do |article| %>
              <%= link_to article_path(article) do %>
                <div class="article-info mb-3">
                  <div class="d-flex justify-content-between">
                      <%= cl_image_tag article.photos.first.key, width: 898, height: 400, crop: :fill %>
                  </div>
                  <div class="article-title border-bottom">
                    <p class="flex-grow-1 pl-2 pt-2"><%= article.title %></p>
                    <p class="pr-2 pt-2">Publié le <%= article.created_at %></p>
                  </div>
                  <div class="pl-2">
                    <p><%= article.rich_body %></p>
                  </div>
                </div>
              <% end %>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>
