<div class="container">
  <div class="row">
    <ul>
      <%= @animals.each do |animal| %>
        <%= link_to animal_path(animal) do %>
          <li><%= animal.name %></li>
        <% end %>
      <% end %>
    </ul>
  </div>
</div>
