<div class="showi">
  <%= simple_form_for [@theme, @image] do |f| %>
    <%= f.input :photo, as: :file %>
    <%= f.input :description %>
    <%= f.button :submit, "Ajouter photo", class: "btn btn-success" %>
  <% end %>
</div>
