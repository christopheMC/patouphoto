<div class="formul-new-theme d-flex align-items-center justify-content-center">
  <%= simple_form_for(@theme) do |f| %>
    <%= f.input :name %>
    <%= f.button :submit, "Créer un thème", class: "btn btn-success" %>
  <% end %>
</div>
